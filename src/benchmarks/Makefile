all: mutex_benchmark

mutex_benchmark:
	@echo -e "\x1B[34m############## compiling mutex_benchmark ##############\x1B[0m"
	@(cd mutex && make)

clean:
	@echo -e "\x1B[34m############## cleaning mutex_benchmark ##############\x1B[0m"
	@(cd mutex && make clean)
